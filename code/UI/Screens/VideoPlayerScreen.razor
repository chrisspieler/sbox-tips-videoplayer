@using Sandbox.UI;
@inherits PanelComponent

<root>
    @if ( CurrentVideo is not null)
    {
        <AttributionPanel Source=@CurrentVideo/>
    }
    <VideoPlayerPanel VideoPath=@CurrentVideoPath/>
</root>
